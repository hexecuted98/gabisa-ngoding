/**
 * @file contoh_lengkap.cpp
 * @brief Contoh penggunaan lengkap lexer -> parser -> compiler -> VM
 */

#include "src/utilitas/gabisa-ngoding.converter.hpp"

int main() {
    std::string teks = "Hello world for the world!";
    stringToGabisaNgoding(teks);
}